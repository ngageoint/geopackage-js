/// <reference types="node" />
import { TileCreator } from './tileCreator';
import { TileMatrix } from '../matrix/tileMatrix';
import { TileMatrixSet } from '../matrixset/tileMatrixSet';
import { BoundingBox } from '../../..';
import { SpatialReferenceSystem } from '../../core/srs/spatialReferenceSystem';
export declare class NodeTileCreator extends TileCreator {
    canvas: any;
    pixelAdded: boolean;
    Canvas: any;
    ctx: any;
    tileCanvas: any;
    tileContext: any;
    imageData: any;
    constructor(width: number, height: number, tileMatrix: TileMatrix, tileMatrixSet: TileMatrixSet, tileBoundingBox: BoundingBox, srs: SpatialReferenceSystem, projectionTo: string, canvas?: any);
    initialize(): Promise<NodeTileCreator>;
    addPixel(targetX: number, targetY: number, sourceX: number, sourceY: number): void;
    addTile(tileData: any, gridColumn: number, gridRow: number): Promise<any>;
    getCompleteTile(format?: string): Promise<Buffer>;
}
