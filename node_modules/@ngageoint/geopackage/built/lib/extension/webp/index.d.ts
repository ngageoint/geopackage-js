import { GeoPackage } from '../../geoPackage';
import { BaseExtension } from '../baseExtension';
import { Extension } from '../extension';
export declare class WebPExtension extends BaseExtension {
    static readonly EXTENSION_NAME: string;
    static readonly EXTENSION_WEBP_AUTHOR: string;
    static readonly EXTENSION_WEBP_NAME_NO_AUTHOR: string;
    static readonly EXTENSION_WEBP_DEFINITION: string;
    constructor(geoPackage: GeoPackage, tableName: string);
    getOrCreateExtension(): Promise<Extension>;
}
